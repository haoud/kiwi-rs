[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[workspace]
members = ["arch"]

[dependencies]
arch = { path = "arch" }

[features]
default = []

[profile.release]
codegen-units = 1
opt-level = "s"
lto = true
